# ML.lab4
In this lab, several data preprocessing techniques were applied to improve data quality.  
Missing values and outliers were handled, and normalization was performed to scale the data.  
Finally, PCA was used to reduce dimensionality while preserving important information.  
These steps are essential for preparing data for machine learning models.
